#include "monty.h"

/**
 * s_stack - sets the format of the data to a stack (LIFO)
 * @head: pointer to the head of the stack
 * @counter: line number
 */
void s_stack(stacky_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	dis.change = 0; /* 0 means stack (LIFO) */
}
#include "monty.h"

/**
 * s_stack - sets the format of the data to a stack (LIFO)
 * @head: pointer to the head of the stack
 * @counter: line number
 */
void s_stack(stacky_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	dis.change = 0; /* 0 means stack (LIFO) */
}
